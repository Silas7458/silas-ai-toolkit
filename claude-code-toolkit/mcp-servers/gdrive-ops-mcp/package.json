{
  "name": "gdrive-ops-mcp",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing Google Drive write ops (move/rename/create-folder/trash/restore) plus Google Doc create/read/locate/insert/replace with normalized matching, count guards, dry-run and atomic batch edits. Wraps the gws CLI. No permanent delete by design.",
  "main": "server.js",
  "scripts": {
    "selftest": "node selftest.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
